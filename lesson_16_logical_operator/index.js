// logical operators && is and || is or ! is not; not true is false and vise versa

const temprature = 20;

if (temprature > 0 && temprature < 30) {
  console.log("The temprature is good!");
} else {
  console.log("The Temperature is bad!");
}
